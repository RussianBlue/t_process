<tr>
  <td class="center"><%= board.pre_title %></td>
  <td class="center"><%= current_dashboard_category(board.category_id) %></td>
  <td>
    <%= link_to board.title, notice_path(current_project, board, :category_id => board.category_id, :curriculum_id => board.curriculum_id) %>
    
    <% if new_tag(board.created_at) == true %>
      <span class="label label-warning">NEW</span>
    <% end %>
  </td>
  <td class="center"><%= board.user.name %></td>
  <td class="center"><%= board.created_at %></td>
  <td class="center"><%= board.count_at %></td>
</tr>