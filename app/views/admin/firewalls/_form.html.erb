<%= simple_form_for(@firewall) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :remote_ip %>
    <%= f.input :ip_access %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
